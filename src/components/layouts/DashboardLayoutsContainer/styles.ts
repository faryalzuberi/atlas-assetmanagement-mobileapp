import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    marginVertical: 10,
  },
  bodyContainer: {
    flex: 4,
    flexDirection: 'column',
    paddingBottom: 45,
  },
});
