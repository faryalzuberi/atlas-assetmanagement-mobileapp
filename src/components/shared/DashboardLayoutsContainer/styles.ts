import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    marginHorizontal: -10,
    marginVertical: 10,
  },
  bodyContainer: {
    flex: 1,
    flexDirection: 'column',
    paddingBottom: 85,
  },
});
