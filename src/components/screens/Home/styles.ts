import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  bodyContainer: {
    paddingHorizontal: 10,
    paddingVertical: 10,
  },
  cardContainer: {
    flex: 1,
    flexDirection: 'row',
  },
});
