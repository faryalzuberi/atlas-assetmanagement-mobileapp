import React from 'react';
import { Text } from 'react-native';
import { LoginLayoutContainer } from '../../../layouts/LoginLayoutContainer';

export const Login = () =>{
    return (
        <LoginLayoutContainer />
    )
}